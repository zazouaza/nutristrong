a full stack project made with typescript and node js 
with nginx configured as a loadbalancer and a ci cd piepline configured to deploy to docker hub " create 4 images 2 for the frontend and 2 for backend" with minikube as an oorchestrator to manage the containers 
